(()=>{const data=window.SCREENMATCH_DATA||[],q=document.getElementById("query"),r=document.getElementById("results"),c=document.getElementById("clearBtn"),bs=[...document.querySelectorAll(".type-btn")];let type="screen";const n=s=>(s||"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim();function score(i,x){const z=n(x);if(!z)return 0;const f=[i.model,i.brand,...(i.aliases||[])].map(n);if(f.includes(z))return 100;if(f.some(a=>a.startsWith(z)))return 80;if(f.some(a=>a.includes(z)))return 60;return z.split(" ").every(p=>f.some(a=>a.includes(p)))?40:0}function render(){if(!q.value.trim()){r.innerHTML='<div class="empty-state"><h2>Search a model</h2><p>Try “iPhone 12”, “iPhone 13 Pro”, “A70”, or “A15”.</p></div>';return}const m=data.map(i=>({i,s:score(i,q.value)})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s).slice(0,6);if(!m.length){r.innerHTML='<div class="empty-state"><h2>No match yet</h2><p>This model is not in Version 1's database yet.</p><p class="note">Add it in <strong>data.js</strong>.</p></div>';return}r.innerHTML=m.map(({i})=>`<article class="result-card"><div class="result-head"><h2>${i.model}</h2><p class="brand">${i.brand} • ${type==="screen"?"Screen protector":"Camera protector"}</p></div><div class="matches">${(i[type]||[]).map(x=>`<div class="match"><div class="match-top"><span class="match-name">${x.model}</span><span class="badge ${x.level}">${x.label}</span></div><p class="reason">${x.reason}</p></div>`).join("")}</div>${i.note?`<div class="note">${i.note}</div>`:""}</article>`).join("")}q.addEventListener("input",render);c.addEventListener("click",()=>{q.value="";q.focus();render()});bs.forEach(b=>b.addEventListener("click",()=>{type=b.dataset.type;bs.forEach(x=>x.classList.toggle("active",x===b));render()}));if("serviceWorker" in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("sw.js").catch(()=>{}));})();